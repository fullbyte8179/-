Задание 1: Вариант 11

Формулировка задачи: Напишите функцию для генерации всех перестановок строки.

Теоретическое обоснование (на основе методических материалов)
Задача генерации всех перестановок строки является классической комбинаторной задачей. В предоставленных методических указаниях (раздел 4.2 "Генерация всех перестановок") подробно разобран алгоритм для генерации перестановок множества целых чисел. Этот же алгоритм полностью применим и для строки, так как строку можно рассматривать как упорядоченное множество (массив) символов.
Ключевым методом для решения этой задачи является рекурсия с возвратом (backtracking), концепция которой описана в разделе 5.1 "Концепция backtracking".
Алгоритм работает следующим образом:
Функция-обертка: Основная функция, которая инициализирует процесс. Она вызывает рекурсивную функцию, передавая ей исходную строку и начальный индекс (обычно 0).
Рекурсивная функция generate(string, index):
Базовый случай (Base Case): Как указано в разделе 1.1, рекурсия должна иметь условие завершения. В нашей задаче базовый случай наступает, когда index (текущая позиция, которую мы "фиксируем") достигает конца строки. Это означает, что одна полная перестановка сформирована, и мы можем ее сохранить или вывести на экран.
Рекурсивный случай (Recursive Case): Для текущей позиции index мы перебираем все символы от index до конца строки.
Шаг вперед (Выбор): Мы меняем местами (swap) символ на позиции index с символом на позиции i (где i — индекс в цикле). Таким образом, мы "фиксируем" новый символ на index-ой позиции.
Рекурсивный вызов: Вызываем ту же функцию для следующей позиции generate(string, index + 1).
Шаг назад (Откат): После того как рекурсивный вызов вернулся (т.е. все перестановки для подстроки index+1 были сгенерированы), мы обязательно должны отменить сделанное изменение. Мы снова меняем местами (swap) символы на позициях index и i, возвращая строку в исходное состояние. Этот шаг, названный в методичке "Откат изменений (backtrack)", критически важен, чтобы на следующей итерации цикла мы могли зафиксировать другой символ на позиции index.
Временная сложность алгоритма составляет O(n! × n), так как существует n! перестановок, и для генерации (и вывода) каждой из них требуется время, пропорциональное ее длине n.

Реализовал функцию в папкаx python1.py, java1.jv, c++1.cpp
